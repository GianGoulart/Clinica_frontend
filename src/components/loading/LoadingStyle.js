const LoadingStyle = () => {
    return {
        circularProgressWrapper: {
            position: 'relative',
            minHeight: '100%',
            minWidth: '100%',
        },
        circularProgress: {
            position: 'absolute',
            top: 0,
            left: 0,
            right: 0,
            bottom: 0,
            margin: 'auto',
        },
    }
}

export default LoadingStyle;
